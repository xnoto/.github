# xnoto community defaults

This public repository provides default community-health files for repositories owned by [@xnoto](https://github.com/xnoto).

GitHub uses these files only when an individual repository does not provide its own equivalent file. Repository-local guidance and templates take precedence.

This repository's README is not the account profile README. A profile README must live in a separate public repository named `xnoto`.
